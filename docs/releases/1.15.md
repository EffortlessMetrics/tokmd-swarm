# tokmd 1.15

Status: RC3 consumer proof passed; stable `v1.15.0` preparation is in progress.

## Release thesis

tokmd 1.15 makes repository evidence portable, consumable, and easier to
operate. It improves CLI configuration, progress, help, recovery, and
argument consistency; adds deterministic snapshot and fail-closed ZIP
ingestion across native and browser/WASM paths; strengthens packet consumption
for review workflows; and makes the Action/container path reproducible and
version-safe.

## User-facing capabilities

### CLI and configuration UX

- Root options are either genuinely shared or rejected clearly; they are not
  parsed and silently ignored.
- Scan options work in the documented positions, generated help covers every
  command, typo suggestions use the live command inventory, and output flags
  use canonical `--output` and `--output-dir` spellings with compatibility
  aliases.
- `--show-config`, progress routing, and per-preset help make the first useful
  command easier to understand and reproduce.

### Portable evidence and browser/WASM

- Repository snapshots can be consumed from memory and archives.
- ZIP admission is deterministic and fail-closed for native and browser/WASM
  callers.
- The release artifact is built with archive support and the browser runner is
  tested against that capability.

### Packets, Action, and container runtime

- Packet generation and ub-review consumption expose deterministic manifests,
  artifacts, status, and bounded claim surfaces.
- Omitted Action versions resolve to the exact version carried by the selected
  Action ref; explicit versions remain available for RCs and rollback.
- Container execution accepts only verified concrete tags. A pre-tag candidate
  image is anonymously exercised and its digest is promoted without rebuild
  before the exact release tag is verified.

### Evidence and review signals

Syntax, shadow, and review-priority evidence is more useful and better
documented, but remains advisory unless a downstream policy explicitly
promotes it.

## Adoption examples

```text
tokmd --show-config
tokmd module --format json
tokmd packet generate --no-syntax
```

```yaml
- uses: EffortlessMetrics/tokmd@v1
  with:
    mode: packet
```

Use an explicit `version` only when selecting an RC, demonstrating rollback,
or selecting a separately verified concrete container tag.

## Non-claims

- tokmd does not prove absence of undefined behavior or memory-safety defects.
- tokmd does not make an automatic merge verdict.
- AST, syntax, mutation, coverage, and proof evidence are not promoted by
  default.
- Homebrew, AUR, Scoop, and WinGet prototype files are not supported 1.15
  distribution channels.
- No container tag is supported before its exact candidate and anonymous
  runtime verification receipts pass.

## Release links

- [1.15 readiness](1.15-readiness.md)
- [1.15 ledger](1.15-ledger.md)
- [Changelog](../../CHANGELOG.md)
