# tokmd 1.14 Release Ledger

## Purpose

tokmd 1.14 is the PR evidence-packet workflow release. It promotes the
`sensors/tokmd/` evidence packet from a manual multi-step recipe into a
first-class local and CI workflow via `tokmd render`, `tokmd packet generate`,
and the `mode: packet` GitHub Action. It also sharpens syntax-receipt and
complexity evidence and makes several CLI error and validation paths more
actionable.

This ledger is the maintainer release record. It is intentionally narrow: the
1.14 cycle does not change any receipt schema version, add a reachability or
safety proof, or perform release mutation from `tokmd-swarm`. Tagging, GitHub
release creation, crates.io publication, and GHCR publication happened from the
publication repository `EffortlessMetrics/tokmd`.

## Source Range

- Previous stable release: `v1.13.1`
- Released version: `1.14.0`
- Release tag commit: `f5f810eb24de214aee19da2f902bd1a829d2d9d0`
  (annotated tag object `ef132fa713b9302d01e7e1913f63b06b2f9eefa4`)
- Release readiness companion: [1.14 readiness](1.14-readiness.md)
- Pre-release checks: [release readiness](../release-readiness.md)
- Workbench origin: prepared on `release/1.14.0-prep`, validated on
  `tokmd-swarm` PR
  [#313](https://github.com/EffortlessMetrics/tokmd-swarm/pull/313), then
  imported into `EffortlessMetrics/tokmd` by merge commit before tagging.

## How To Read This Ledger

Use `CHANGELOG.md` for the concise release index, this ledger for the maintainer
distribution record, and [1.14 readiness](1.14-readiness.md) for the
command-backed pre-release readiness report.

## Distribution Record

The following artifacts were published for `v1.14.0`. The receipts below were
captured during post-release housekeeping on 2026-06-25.

| Surface | Status | Receipt |
| --- | --- | --- |
| Git tag | Published | Annotated tag `v1.14.0` → commit `f5f810eb24de214aee19da2f902bd1a829d2d9d0`. |
| GitHub release | Published | <https://github.com/EffortlessMetrics/tokmd/releases/tag/v1.14.0> (not draft, not prerelease; published `2026-06-25T18:42:02Z`). |
| Release workflow | Success | <https://github.com/EffortlessMetrics/tokmd/actions/runs/28192117926> (`release.yml`, branch `v1.14.0`, head `f5f810eb`, conclusion `success`). |
| crates.io | Published | `tokmd` `1.14.0` present and not yanked (`created_at 2026-06-25T18:48:13Z`, `crate_size 163974` bytes) via the crates.io API. Lockstep workspace crates publish at the same version. |
| GHCR (publication) | Advisory pass | `ghcr.io/effortlessmetrics/tokmd:1.14.0` returned HTTP 200 to an unauthenticated registry pull token. Manifest is an OCI image index, digest `sha256:bd214464eb9c8df734e88dd64e81fab072021e39ec1b3a23b69eebbfb914b096`. |
| npm | Not published | `@tokmd/*` was intentionally **not** published this cycle. |
| PyPI | Not published | `tokmd` (PyPI) was intentionally **not** published this cycle. |
| Install smoke | Pass | `cargo install tokmd --version 1.14.0` from crates.io built and installed `tokmd v1.14.0`; the installed binary reports `tokmd 1.14.0` and exposes the new `render` and `packet generate` commands in the default feature set. |

### crates.io confirmation

`tokmd 1.14.0` is the live crates.io version, unyanked. The release follows the
repository's lockstep microcrate model, so the publishable workspace crates
share the `1.14.0` version. This receipt confirms the top-level `tokmd` crate;
it does not independently re-verify every dependent crate page.

### GHCR visibility receipt (advisory)

The GHCR row records an **advisory** unauthenticated manifest fetch only. A
fresh anonymous pull token from `ghcr.io/token` resolved
`ghcr.io/effortlessmetrics/tokmd:1.14.0` to an OCI image index (HTTP 200,
digest `sha256:bd214464eb9c8df734e88dd64e81fab072021e39ec1b3a23b69eebbfb914b096`).

This is the post-release advisory signal described in
[release readiness — Post-Release GHCR Visibility](../release-readiness.md#post-release-ghcr-visibility).
Recording the formal `verified-public` maintainer receipt for the GHCR package
(per the
[Publishing evidence — Post-Release GHCR Visibility Checks](../publishing-evidence.md#post-release-ghcr-visibility-checks)
template) remains a maintainer-only action. The swarm workbench GHCR surface
(`ghcr.io/effortlessmetrics/tokmd-swarm`) is tracked separately (issue #264).

### Post-release install smoke

A clean `cargo install tokmd --version 1.14.0` from crates.io (into an isolated
`--root`) built and installed `tokmd v1.14.0` on Windows MSVC. The installed
binary reported `tokmd 1.14.0`, and `tokmd render --help` and
`tokmd packet generate --help` both resolved, confirming the new packet-workflow
CLI surface is present in the **default** install feature set (the
`v1.13.1`-style default-feature regression did not recur). This smoke proves the
crate installs and exposes the documented commands; it does not exercise a full
packet run or prove runtime correctness.

### npm / PyPI

npm and PyPI publication were explicitly out of scope for the `1.14.0` cycle.
No `@tokmd/*` npm package and no `tokmd` PyPI wheel were published for this
version. The next binding publication is a separate, explicit maintainer
decision.

## Lane Summary

| Lane | Outcome | User impact |
| --- | --- | --- |
| Packet rendering CLI | Added `tokmd render` to render packet presets (including unsafe-review bundles) from analysis/context/syntax inputs, ingest sibling tool bundles, and validate against the `tokmd-packets` JSON Schema at the CLI boundary. | Consumers can build validated packet presets from one command. |
| Packet orchestrator CLI | Added `tokmd packet generate`, a one-command evidence-packet orchestrator that produces the `sensors/tokmd/` packet (analysis, context, optional syntax, manifest). | The evidence packet is boring to generate locally. |
| `mode: packet` Action | Added `mode: packet` to the `EffortlessMetrics/tokmd` GitHub Action with prebuilt-binary runtime default, base-ref inference, packet artifact upload, a job summary, and packet status/manifest/dir/count outputs plus `fail-on` status mapping. | CI can produce and gate on evidence packets without bespoke steps. |
| Syntax receipt evidence | Added `panic_seam_summary` to syntax receipts and deprioritized test-assertion noise in syntax evidence ranking. | Packet consumers get higher-value advisory review signals. |
| Complexity correctness | Aligned `max_cyclomatic` with per-function complexity units. | The reported maximum reflects a single function, not an aggregate. |
| CLI actionability | Made `tokmd diff` `resolve_targets` errors actionable, added numeric-flag validation for `tokmd context` / `tokmd handoff`, and detect UTF-16 JSON inputs (bare PowerShell `>` redirect) in packet/evidence-packet ingestion. | Common CLI mistakes now produce clear, fixable errors. |
| Security | Upgraded `pyo3` (and its PyO3 crate family) `0.28.3 → 0.29.0` in `tokmd-python`, resolving `RUSTSEC-2026-0176` flagged by `cargo deny`. | The Python binding dependency surface clears the advisory. |
| Docs | Added usage example blocks to `tokmd module`, `run`, and `init` help and regenerated `docs/reference-cli.md`. | `--help` shows common invocations consistently. |
| CI economics | Retired the duplicate routed Rust Small frontdoor workflow and its lane catalogue entries; routing is now the advisory `Route CI runner` job feeding the single required `Tokmd Rust Result` gate. | No required check changed; the duplicate lane is gone. |

See the `## [1.14.0]` section of `CHANGELOG.md` for the full user-facing entry.

## Validation Pattern

The release prep validated on Windows from `release/1.14.0-prep` and on
`tokmd-swarm` PR #313 before publication import. Recorded in
[1.14 readiness](1.14-readiness.md):

- full workspace tests (`cargo test --workspace`);
- `cargo fmt-check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings`;
- strict no-panic family checker (`cargo xtask check-no-panic-family --strict`);
- the required `Tokmd Rust Result` gate on PR #313;
- two stale-assertion fixes (CycloneDX SBOM version snapshots and a CI checkout
  action version assertion) made during prep.

Post-release distribution verification (this ledger) confirmed the GitHub
release, the successful release workflow run, the crates.io `1.14.0` version, and
the advisory GHCR manifest fetch above.

## Schema Versions (unchanged)

No receipt schema version changed in `1.14.0`:

- `SCHEMA_VERSION = 2`
- `ANALYSIS_SCHEMA_VERSION = 9`
- `COCKPIT_SCHEMA_VERSION = 3`
- `HANDOFF_SCHEMA_VERSION = 5`
- `CONTEXT_SCHEMA_VERSION = 4`
- `CONTEXT_BUNDLE_SCHEMA_VERSION = 2`

## Deferred / Not Claimed

1.14 does not:

- change any receipt schema version;
- add undefined-behavior detection, reachability proof, safety proof, or a
  semantic call graph;
- publish npm or PyPI artifacts;
- claim a formal GHCR `verified-public` maintainer receipt (the GHCR row here is
  an advisory unauthenticated fetch only);
- perform tag, GitHub release, crates.io, image, or alias mutation from
  `tokmd-swarm`.

## Claim Boundary

This ledger records the published `v1.14.0` distribution surface and the
post-release receipts captured on 2026-06-25: the GitHub release, the successful
release workflow run, the crates.io `1.14.0` version, and an advisory
unauthenticated GHCR manifest fetch. It does not claim a formal GHCR
maintainer receipt, does not claim npm/PyPI publication (intentionally skipped),
and does not prove runtime safety, mutation adequacy, or coverage.
