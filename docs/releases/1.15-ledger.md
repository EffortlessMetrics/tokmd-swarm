# tokmd 1.15 Release Ledger

Status: preparation ledger. Pending entries are intentionally explicit; a
placeholder is not a release receipt.

## Source and topology

| Field | Receipt |
| --- | --- |
| Previous stable | `v1.14.0` |
| RC target | `v1.15.0-rc.1` |
| Stable target | `v1.15.0` |
| Final source commit | Pending release-prep PR |
| Publication merge commit | Pending controlled import |
| Graph alignment | Pending `cargo xtask repo-graph --expect aligned` |

## Artifact ledger

| Surface | Exact reference or digest | Verification |
| --- | --- | --- |
| Linux amd64 binary | Pending | Not run |
| Linux arm64 binary | Pending | Not run |
| macOS amd64 binary | Pending | Not run |
| macOS arm64 binary | Pending | Not run |
| Windows amd64 binary | Pending | Not run |
| Archive-enabled WASM | Pending | Not run |
| Checksums | Pending | Not run |
| Build provenance | Pending | Not run |
| Nix | Pending | Not run |

## Action and container ledger

| Surface | Receipt |
| --- | --- |
| `@v1.15.0` Action | Pending exact tag |
| `@v1` Action alias | Must remain unmoved until stable gates pass |
| Candidate container tag | `candidate-<final-commit-sha>`, pending dispatch |
| Verified candidate marker | `verified-candidate-<final-commit-sha>`, pending proof |
| Exact RC/stable container digest | Pending candidate receipt |
| Mutable GHCR aliases | Must not move for RC; stable pending final proof |

## Publication ledger

| Surface | RC policy | Stable receipt |
| --- | --- | --- |
| GitHub prerelease | Publish | Pending |
| crates.io | Do not publish | Pending stable publication |
| GHCR exact tag | Promote verified digest | Pending |
| GHCR `1`, `1.15`, `latest` | Do not move | Pending stable gates |
| Nix | Verify exact RC | Pending |

## Browser and installed-binary ledger

- Exact archive-enabled WASM browser ZIP smoke: pending.
- Linux, Windows, and macOS installed-binary smoke: pending.
- `cargo install tokmd --version 1.15.0` consumer smoke: pending stable.
- Action packet status, artifacts, and `tokmd-version` output: pending.

## Non-claims and inactive channels

Syntax, AST, mutation, coverage, and proof evidence remain advisory unless a
downstream policy promotes them. This release makes no UB-absence claim and no
automatic merge verdict. The inactive Homebrew, AUR, Scoop, and WinGet
templates are removed from the supported release surface rather than updated
with unverifiable hashes.

## Post-release closeout

After stable verification, record GitHub assets, checksums, attestations,
representative crates, Cargo install, Nix, Action binary/container, GHCR exact
and mutable tags, and browser/WASM results. Then restore `## [Unreleased]`,
update `NOW`, `NEXT`, and `ROADMAP`, and leave this ledger as the durable
release receipt.
