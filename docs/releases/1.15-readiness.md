# tokmd 1.15 Release Readiness

Status: stable `v1.15.0` is published and consumer-proven. The initial
crates.io interruption was recovered from the immutable tag and is recorded in
[`1.15.0-incident.md`](1.15.0-incident.md).

## Release boundary

- Stable tag: `v1.15.0`.
- Stable source: `70113a874583b36f6aedabbf9f70b62435314326`.
- Publication/control head after closeout imports: `b4ff2c948d9abeaf5c57f8bc27de02803ef7788b`.
- Final graph: `publication_ahead=0`, `swarm_ahead=0`.
- All tag, release, crates.io, GHCR, and Action alias mutations occurred in
  `EffortlessMetrics/tokmd`.

## Consumer acceptance

Stable consumer workflow `30965258655` passed the complete required matrix:
all advertised binaries, checksums, attestations, Nix, released archive-enabled
WASM browser ZIP, exact Action binary/container packet modes, and the aggregate
fail-closed receipt. Alias promotion workflow `30965139577` passed exact digest
verification, all GHCR aliases, and Action `v1` movement.

The clean external installation and real-Git packet smoke passed locally. The
per-crate registry result is generated from the repository publish plan in
[`1.15.0-registry-inventory.json`](1.15.0-registry-inventory.json).

## Preflight evidence

| Check | Result |
| --- | --- |
| Release source/version consistency | Pass in stable release path |
| Release assets, checksums, attestations | Pass: consumer run `30965258655` |
| Platform binaries | Pass: consumer run `30965258655` |
| Archive-enabled WASM browser smoke | Pass: consumer run `30965258655` |
| Nix exact-tag build/run | Pass: consumer run `30965258655` |
| Action binary/container packet smoke | Pass: consumer run `30965258655` |
| crates.io publication | Pass after recovery run `30956348205` |
| Clean Cargo install | Pass: `tokmd 1.15.0` |
| Full local workspace test | Not proven: isolated Windows run stopped under concurrent Cargo workload |
| `cargo xtask publish --dry-run` | Not proven: package validation timed out under concurrent Cargo workload |

The two local `not proven` entries are retained as evidence gaps rather than
silently promoted by the hosted release receipts.

## Claim boundary

This readiness record proves the published stable consumer surfaces and
registry inventory. It does not prove absence of undefined behavior, promote
AST/syntax/mutation/coverage evidence, or support inactive package-manager
templates. The release-order repair for `v1.15.1` is a follow-up control lane.
