# tokmd 1.15 Release Readiness

Status: `v1.15.0-rc.1` published as a GitHub prerelease from the aligned
publication commit. The release workflow passed its required RC jobs; this
document records the exact receipts and keeps consumer checks that were not
run explicitly open.

## Release boundary

- Target: `v1.15.0-rc.1`, followed by `v1.15.0`.
- Previous stable: `v1.14.0`.
- Preparation repository: `EffortlessMetrics/tokmd-swarm`.
- Publication repository: `EffortlessMetrics/tokmd`.
- Tagging, publishing, GHCR mutation, and Action alias movement occur only in
  the publication repository.

## Required preflight

The following commands were run against the final release-prep source before
publication. The full workspace test and publish dry-run commands exceeded the
local Windows execution budget and remain `not run` evidence, not failures.

```text
cargo fmt-check
cargo gate-check
cargo xtask version-consistency
cargo xtask publish-surface --json --verify-publish
cargo xtask doc-artifacts --check
cargo xtask docs --check
cargo xtask proof-policy --check
cargo xtask check-no-panic-family --strict
cargo test --workspace --all-features
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo deny --all-features check
cargo xtask publish --dry-run
npm --prefix web/runner test
npm --prefix web/runner run build:wasm:archive-zip
```

Receipt: `b02c6a4bd1f9948df4a8763fc053cdff23fb8c57`. Passed: formatting,
gate-check, version consistency, publish surface, documentation and proof
checks, strict panic-family policy, Clippy, cargo-deny, archive-enabled WASM
build, and `web/runner` tests (69 passed). `cargo test --workspace
--all-features` and `cargo xtask publish --dry-run` were attempted locally but
timed out and are not claimed as passes. The required hosted release workflow
completed successfully as run `30858987393`.

## Artifact consumer proof

The RC receipt must use the exact published artifacts, not locally rebuilt
approximations:

| Surface | Required evidence | Status |
| --- | --- | --- |
| Platform binaries | Each reports `1.15.0-rc.1` | Published in RC run `30858987393`; Windows exact artifact smoke passed, Linux/macOS smoke pending |
| CLI | `--version`, `--help`, `--show-config`, module, packet, invalid-option smoke | Pass: exact published Windows RC binary; `--version`, help, config, JSON module, and invalid root option exit 2 |
| Action binary | Omitted and explicit RC version paths report the RC | Pass: exact-tag Action verification in RC run `30858987393` |
| Candidate container | Anonymous pull/version/mounted packet; immutable digest receipt | Pass: exact source `b02c6a4b`; proof run `30858473060`; receipt artifact `container-candidate-b02c6a4bd1f9948df4a8763fc053cdff23fb8c57` |
| RC container tag | Exact digest promotion and re-verification | Pass: exact candidate digest promoted and verified in RC run `30858987393` |
| Archive WASM | Exact release archive passes browser ZIP smoke | Published as `tokmd-wasm-v1.15.0-rc.1.tar.gz`; local archive build and 69/69 runner tests pass; released-archive browser smoke pending |
| crates.io | Not published for RC | Policy |
| Nix | Build and run exact RC source/artifact | Not run |
| Checksums and attestations | Present for every published artifact | Published by RC run `30858987393`; consumer retrieval/verification pending |

## Publication topology

The final swarm tip was imported into the publication repository with one true
two-parent merge commit `b02c6a4bd1f9948df4a8763fc053cdff23fb8c57` and the
swarm main ref was fast-forwarded to it. The graph proof passed with
`publication_ahead=0` and `swarm_ahead=0` before tagging.

## Claim boundary

This document records a published RC, not stable-release readiness. It makes
no claim that crates.io publication, mutable aliases, Nix, cross-platform
installed-binary smoke beyond the Windows exact artifact, or released-archive
browser smoke has passed.

Exact Windows RC receipt: downloaded `tokmd-windows-amd64.exe` from
`v1.15.0-rc.1`; `--version`, `--help`, `--show-config`, `module --format
json`, and `--format json module` were run. The last command exited `2`,
confirming the root option is rejected instead of silently ignored.
