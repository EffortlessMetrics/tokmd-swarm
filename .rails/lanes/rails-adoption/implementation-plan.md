# Implementation plan: rails-adoption

## Objective

Introduce a durable `.rails/` framework footprint, templates, and initial artifact graph while keeping external namespaces awareness-only.

## Sequence

1. Add `.rails/` directories, root README, and index conventions.
2. Add docs guidance in `docs/rails.md` and `docs/contributing/rails.md`.
3. Add reusable templates for proposals/specs/ADRs/lane/closeout and support/policy references.
4. Seed initial proposal, ADR, spec, and lane tracker artifacts.

## Proof plan

- `git diff --check`
